{
  "$schema":"http://json-schema.org/draft-07/schema",
  "description": "The base schema for all the individual endpoints, used to provide reference",
  "type": "object",
  "definitions": {
    "messagePacket": {
      "required": [ "data", "text", "type" ],
      "properties": {
        "data": { "type": "string" },
        "text": { "type": "string" },
        "type": { "type": "string" }
      }
    }
  },
  "properties": {
    "messagePacket": { "type": "object", "$ref": "#/definitions/messagePacket" }
  }
}